server:
  port: 8080

# Admin login credentials (can be overridden via environment variables)
app:
  admin:
    email: ${ADMIN_EMAIL:admin@gmail.com}
    password: ${ADMIN_PASSWORD:12345}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bus
    username: root
    password: Swaminarayan20

  jpa:
    hibernate:
      ddl-auto: none   # IMPORTANT: you already have tables
    show-sql: true
    open-in-view: false